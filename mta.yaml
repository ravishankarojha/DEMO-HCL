_schema-version: "2.0.0"
ID: "com.sap.teched2019.cna370.fiori"
version: 1.0.0

parameters:
  hcp-deployer-version: "0.0.0"

modules:
  - name: "fiori201"
    type: html5
    path: .
    parameters:
       version: 1.0.0-${timestamp}
    build-parameters:
       builder: grunt
       build-result: dist 
